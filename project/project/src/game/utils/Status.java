package game.utils;

/**
 * Use this enum class to give `buff` or `debuff`.
 * Created by:
 * @author Riordan D. Alfredo
 */
public enum Status {
    HOSTILE_TO_ENEMY, //gives the hostile to enemy typing to an actor
    RESPAWNABLE, //gives the respawnable typing to an actor
    RESTING, //gives the resting typing to an actor
    WIPED, //gives the wiped typing to an actor
    FRIENDLY
}
